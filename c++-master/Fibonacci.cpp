using namespace std;

#include<iostream>
#include <vector>

int getMaxProfit(vector<int>& profit, vector<int>&weight, int n, int capacity){

    vector<vector<int> ()
}

int main(int argc, char* argv[]){
  vector<int> profit = {0,1,2,5,6};
  vector<int> weight = {0,2,3,4,5};

  int capacity = 8;
  int n = profit.size();

  int maxProfit = getMaxProfit(profit, weight, n, capacity);

  return maxProfit;
}